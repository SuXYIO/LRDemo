pn/printneuron [args]
	Print values for neuron. 
	If name not given, print for all names. 
	If valuestring not given, print for all values. 
	Options: 
		-n [name]
			Define to print values for which neuron. 
			name: 
				f / g / a
				(neuron f, neuron g, all)
			Default is print all. 
		-f [formatstring]
			Print in a format. 
			Put all the valuechars you want in the formatstring. 
			valuechars: 
				%w / %b / %l / %W / %B / %%
				(weight, bias, loss, gradient for weight, gradient for bias, literal '%')
				Note that l / W / B will be always 0.0 for g normally, since it's the constant neruon. 
			Default is "%w, %b, %l, %W, %B\n". 
		-F [filename]
			Print to file. 
		-p [precision]
			Define how many numbers to print after decimal point for floats and doubles. 
			The default value is defined in main.h FPP macro. 
